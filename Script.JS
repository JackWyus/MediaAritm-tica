/*Desenvolva um Programa que Cálcule duas notas de um Aluno e faça a média Aritmética entre os números.*/

    var nota1 = parseFloat(prompt('Digite a Primeira Nota:'));
    var nota2 = parseFloat(prompt('Digite a Segunda Nota:'));

    mediaFinal = (nota1 + nota2)/2;

    alert('A Média Aritmética entre as duas notas é: '+mediaFinal);